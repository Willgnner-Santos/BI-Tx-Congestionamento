services:
  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    container_name: tx-congestionamento-web
    ports:
      - "3000:3000"
    env_file:
      - ./web/.env.local
    restart: unless-stopped
